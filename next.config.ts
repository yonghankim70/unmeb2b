import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["unmeb2b.com", "www.unmeb2b.com", "localhost:3000"],
  devIndicators: false,
};

export default nextConfig;

